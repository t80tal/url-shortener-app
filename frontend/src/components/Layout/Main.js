import React from 'react'

import Wrapper from '../../assets/wrappers/Main'

// Main content
const Main = ({ children }) => {
  return (
    <Wrapper>
      {children}
    </Wrapper>
  )
}

export default Main